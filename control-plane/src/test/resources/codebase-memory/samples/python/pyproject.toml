[project]
name = "sample_python"
version = "0.1.0"
description = "Sample Python project for codebase-memory testing"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
