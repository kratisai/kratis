{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AcpInitializedParams",
  "type": "object",
  "required": ["sessionId", "agentName", "agentVersion", "executionId"],
  "properties": {
    "sessionId": { "type": "string" },
    "agentName": { "type": "string" },
    "agentVersion": { "type": "string" },
    "executionId": { "type": "string" },
    "relaunch": {
      "type": "boolean",
      "description": "True when this handshake replaces a terminated session. The control plane must not re-dispatch the task prompt; the caller that requested the relaunch delivers its own prompt."
    }
  },
  "additionalProperties": false
}
